mixin standartCheckbox(options)
    label.standart-checkbox
        input(type="checkbox", checked=options.isChecked, name=options.name).standart-checkbox__input
        span.standart-checkbox__title=options.title
